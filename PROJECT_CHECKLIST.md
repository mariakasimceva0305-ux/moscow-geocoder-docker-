# Чеклист готовности проекта

## ✅ Что готово

### Репозиторий

- [x] `.gitignore` — правильная конфигурация для исключения ненужных файлов
- [x] Все исходные файлы на месте
- [x] Инструкция по инициализации Git (`SETUP_GIT.md`)

### Docker конфигурация

- [x] `Dockerfile` — готов к сборке
- [x] `docker-compose.yml` — удобный запуск через Docker Compose
- [x] `.dockerignore` — оптимизация сборки
- [x] `DOCKER.md` — подробная инструкция по запуску в Docker

### Документация

- [x] `README.md` — полная документация с:
  - Быстрым стартом
  - Общим описанием подхода
  - Описанием данных и preprocessing
  - Детальным описанием алгоритмов
  - Методологией экспериментов
  - Инструкциями по запуску (Docker и локально)
  - Структурой проекта
- [x] `SCORE_EXPLANATION.md` — подробное объяснение алгоритма скоринга
- [x] `SETUP_GIT.md` — инструкция по настройке репозитория

### Исходный код

- [x] Все модули в `src/`
- [x] CLI скрипты в `scripts/`
- [x] Preprocessing скрипт для подготовки данных
- [x] REST API через FastAPI

### Данные

- [x] `preprocessing.py` — скрипт для подготовки данных из PBF
- [x] Инструкции по подготовке данных
- [x] `moscow_buildings.csv` — готовый файл (или инструкция по созданию)

## 🚀 Быстрый старт для судей

### Самый простой способ (Docker):

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd best_hack

# 2. Запустить (нужен moscow_buildings.csv в корне)
docker-compose up -d

# 3. API доступен на http://localhost:8000/docs
```

### Если данных нет:

```bash
# 1. Поместить PBF в data/data.osm.pbf
# 2. Запустить preprocessing (локально или в Docker)
python scripts/preprocessing.py
# или
docker-compose run --rm geocoder-api python scripts/preprocessing.py

# 3. Запустить API
docker-compose up -d
```

## 📋 Следующие шаги

1. **Инициализировать Git репозиторий** (если ещё не сделано):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **Создать репозиторий на GitHub** (опционально):
   - Создать новый репозиторий на GitHub
   - Добавить remote: `git remote add origin <url>`
   - Запушить: `git push -u origin main`

3. **Проверить Docker сборку**:
   ```bash
   docker-compose build
   docker-compose up
   ```

4. **Проверить работу API**:
   ```bash
   curl "http://localhost:8000/geocode/improved?address=Москва, Тверская улица, 12"
   ```

## 📝 Примечания

- Файл `moscow_buildings.csv` может быть большим (сотни MB)
- Если файл больше 100MB, рекомендуется использовать Git LFS или не включать в репозиторий
- В `.gitignore` CSV файлы исключены по умолчанию — можно добавить явно при необходимости



